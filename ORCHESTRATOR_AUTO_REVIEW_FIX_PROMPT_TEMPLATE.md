Automatic code review left a message.
If it doesn't flag any issues, do nothing.
Otherwise for each issue:
- Address it
- Or ignore it, if it conflicts with user instructions; requests fallbacks/backward compatibilities that are not explicitly asked by user; widens the implementation scope outside of requested changes. Keep user informed about this decision.

Message:
{{message}}
