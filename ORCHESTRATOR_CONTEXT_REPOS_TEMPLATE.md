# Read-only reference repositories

The following repositories are mounted read-only for context at `{{repositoriesPath}}` (alias: `{{repositoriesAliasPath}}`):

{{contextReposList}}

The list may change between user requests.
