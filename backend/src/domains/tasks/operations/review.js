const { attachTaskReviewMethods } = require('./review/methods');
const { normalizeReviewTarget } = require('./review/target');

module.exports = {
  attachTaskReviewMethods,
  normalizeReviewTarget
};
